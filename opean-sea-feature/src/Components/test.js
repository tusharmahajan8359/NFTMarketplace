import React from 'react'
import PropTypes from 'prop-types';
// shortcut for function based component : rfc

const Card = ([number]) => <div className='carousel-card'>{number}</div>

export default Card;
